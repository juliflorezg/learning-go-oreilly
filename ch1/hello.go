package main

import "fmt"

func main() {

	fmt.Println("hello, world!!")

	// use go run hello.go to run this program as a scripting language
	// use go build hello.go to create an executable hello.exe in the same directory
	// alternatively, we can use go build -o hello_world hello.go to change the executables name (we can also change the output location)
}
